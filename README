1. install libpcap-dev, libpcap0.8, libpcap0.8-dbg, and libpcap0.8-dev, libssl-dev
   sudo apt-get install libpcap-dev
   sudo apt-get install libpcap0.8
   sudo apt-get install libpcap0.8-dbg
   sudo apt-get install libpcap0.8-dev
   sudo apt-get install libssl-dev
   
2. reference the pages:
   http://www.devdungeon.com/content/using-libpcap-c
   http://www.tcpdump.org/pcap.html
   http://www.tcpdump.org/sniffex.c -- download sample C program

3. program directory
   lib   -- FEX Trader API library
   src   -- sources and test files

4. setup executiion environment and run
   cd src
   source setup
   make
   ./ftdparse


